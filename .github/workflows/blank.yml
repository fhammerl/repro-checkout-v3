name: tester

on:
  push:
  workflow_dispatch:
jobs:
  test:
    strategy:
      matrix:
        os: [ubuntu-latest, self-hosted]
    name: runner / gotest
    runs-on: ${{ matrix.os }}
    container: alvisisme/docker-ubuntu-non-root-with-utils:latest
    steps:
      - run: whoami
      - uses: actions/checkout@v3.1.0
  
